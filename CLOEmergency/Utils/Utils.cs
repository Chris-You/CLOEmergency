using Newtonsoft.Json.Linq;

namespace CLOEmergency.Utils
{
    public static class Utils
    {
        public static JArray JsonHelper(string employee)
        {
            JArray jArray = new JArray();


            return jArray;
        }
    }
}
